<UserControl x:Class="PWSW_Project_todo_calendar.Pages.UserControl.TaskAdd"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PWSW_Project_todo_calendar.Pages.UserControl"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="520">

    <Border Background="#FFF9FAFB"
            BorderBrush="{StaticResource BorderBrushSoft}"
            BorderThickness="1"
            CornerRadius="14"
            Padding="14"
            Margin="0,0,0,12">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Nazwa -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="Nazwa zadania" Margin="2,0,0,6"/>
                <TextBox x:Name="TbName"
                         Style="{StaticResource ModernTextBox}"/>
            </StackPanel>

            <!-- Termin -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Termin wykonania" Margin="2,0,0,6"/>
                    <DatePicker x:Name="DpDueDate"
                                Style="{StaticResource ModernDatePicker}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Priorytet" Margin="2,0,0,6"/>
                    <ComboBox x:Name="CbPriority"
                              Style="{StaticResource ModernComboBox}">
                        <ComboBoxItem Content="0"/>
                        <ComboBoxItem Content="1" IsSelected="True"/>
                        <ComboBoxItem Content="2"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <!-- Opis -->
            <StackPanel Grid.Row="2" Margin="0,0,0,12">
                <TextBlock Text="Opis" Margin="2,0,0,6"/>
                <TextBox x:Name="TbDesc"
                         Style="{StaticResource ModernTextBox}"
                         Height="110"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>
                <TextBlock Text="Krótki opis lub notatka do zadania."
                           Foreground="{StaticResource MutedTextBrush}"
                           Margin="2,6,0,0"/>
            </StackPanel>

            <!-- Pasek akcji -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Usuń ten task, jeśli został dodany przez pomyłkę."
                           Foreground="{StaticResource MutedTextBrush}"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        x:Name="deleteBtn"
                        Content="Usuń"
                        Style="{StaticResource DangerButtonStyle}"
                        Width="120"
                        Height="38"
                        Margin="12,0,0,0"
                        Click="Remove_Click"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
